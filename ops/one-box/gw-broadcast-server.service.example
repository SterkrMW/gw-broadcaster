[Unit]
Description=Guild War Broadcast Server
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/gw-broadcast-viewer
ExecStart=/usr/bin/npm run server:prod
Restart=always
RestartSec=5
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
